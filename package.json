{
  "name": "ai-image-workflow-mini",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -b",
    "test": "npm run test --workspace @ai-image-workflow/backend"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
